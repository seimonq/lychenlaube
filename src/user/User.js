export default class User {

  constructor(name, email, role, subscriber, userData) {
    this.name = name
    this.email = email
    this.role = role
    this.subscriber = subscriber
    this.userData = userData
  }
}